# STEP 4.1 — 도시 번영도 / 레벨 완료

기준: 원격 main `1edded3`. 이번 작업 범위는 STEP 4.1만이며, STEP 4.2~4.8은 미구현이다.
사용자 요청에 따라 빌드와 핵심 동작 확인만 수행했다.

## 동작

도시 문서 `macro.prosperity`에 누적 정수 점수를 저장한다. 도시 레벨은 점수에서 파생하며 별도로 저장하지 않는다.
게임 하루(24틱) 정산 때 한 번 적립하고, 기존 오프라인 따라잡기도 같은 정산 경로를 사용한다.

하루 적립량은 `round(sqrt(인구) × (0.25 + 0.75 × 입주율) × 재정배율)`이다.
재정배율은 당일 수지가 0 이상이면 1.1, 적자면 0.9다. 인구 1명 미만은 0점, 나머지는 최소 1점이다.
입주율은 기존 만족도·통근·서비스·재해 영향이 반영된 지표다. 별도 만족도 집계를 추가하지 않았다.
누적 점수는 줄지 않으므로 인구 감소나 적자로 잠금해제가 취소되지 않는다. 저장값 상한은 10억 점이다.

| 도시 레벨 | 누적 번영도 | 이름 | 새로 열리는 항목 |
| --- | ---: | --- | --- |
| 1 | 0 | 마을 | 저소득 건물, 소방·경찰·병원·학교, 소공원 |
| 2 | 100 | 소도시 | 중산층 건물, 공원 |
| 3 | 500 | 성장 도시 | 체육시설 |
| 4 | 1,500 | 대도시 | 고소득 건물 |
| 5 | 4,000 | 중심 도시 | 현재 최고 레벨. 추가 시설 없음 |

주거·상업·공업의 신축과 상향 재건축 모두 열린 건물 계층까지만 허용한다.
잠긴 계층의 수요 몫은 열린 계층에 재분배한다. 기존 건물을 철거하거나 기능을 중단시키지 않는다.
시설 메뉴는 요구 도시 레벨과 비활성 상태를 표시하며, 배치 검사와 커서 미리보기에도 같은 제한을 적용한다.
메뉴가 열린 중에 레벨이 올라가도 선택 가능 상태로 바뀐다.
도시 상태판에 레벨·번영도·다음 잠금해제를 표시하고, 잠긴 계층의 수요 막대에 ‘잠김’을 표시한다.

## 기존 도시 / 초기화

- 번영도가 없는 옛 저장본은 최초 로드 때 저장 인구(최대 4,000점)와 보유 건물·시설의 필요 점수 중 최댓값으로 보정한다.
- 예: 고소득 건물이 있으면 최소 도시 레벨 4, 체육시설만 있으면 최소 레벨 3으로 시작한다.
- 현재 게임은 시작 시 대도시를 자동 생성한다. 이 생성 도시에도 같은 보정을 적용하므로, 고소득 건물을 가진 자동 생성 도시는 레벨 4 이상부터 시작할 수 있다. 빈 도시는 레벨 1부터 시작한다.
- 점수가 이미 저장된 도시는 다시 보정하지 않는다. 불러오기만으로 누적 보너스가 반복 지급되지 않는다.
- 도시 초기화 시 번영도 필드를 지운다. 재생성 후 다음 로드에서 새 도시의 보유 건물에 맞춰 보정한다.
- 기존 시설 코드, 청크 저장 구조, 지형 상수, 교통 규칙, 렌더링 구조는 변경하지 않았다.

## 다음 구현 연결점

- `src/sim/progression.ts`: 도시 레벨 구간, 건물·시설 잠금해제 표, 일일 적립, 저장 이행.
- `MacroSim.cityLevel` / `maxBuildingTier`: 후속 인프라의 잠금해제 판단에 사용.
- 새 시설은 기존 시설 체계를 확장하면서 `FACILITY_UNLOCK_LEVEL`에 요구 레벨을 함께 추가해야 한다.
- 저수·취수·배수·하수처리 시설, 배관 저장/렌더링, 혼합 연결 오염과 질병 연계는 다음 STEP 4.2 작업이다.
- 현재 구간과 적립량은 이번 구현에서 정한 초기 밸런스이며 장기 플레이 튜닝은 하지 않았다.

## 검증

- `npm run build` 통과: TypeScript 검사와 Vite 프로덕션 빌드. Pixi 번들 500 kB 초과 경고만 존재.
- `node tools/check/run.mjs progression` 통과: 레벨 경계, 일일 적립, 신축 계층 제한, 시설 잠금, 기존 도시 보정, 기존 건물 보존, 저장 직렬화/복원, 초기화.
- 기존 서비스/재개발 검사 fixture에는 필요한 해제 레벨을 명시했다. 해당 전체 검사들은 이번에 실행하지 않았다.
- 전체 회귀검사, 브라우저 시각 검증, 실제 Firebase 왕복 저장, 장기 밸런스 검증은 수행하지 않았다.

완료 범위: **STEP 4.1**. STEP 4 전체 완료 보고가 아니다.
