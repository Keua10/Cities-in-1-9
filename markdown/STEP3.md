# STEP 3.1 — 매크로 도시 시뮬레이션 (인수인계)

Claude 가 구조와 전체 코드를 작성했다. 이 문서는 **ChatGPT 가 이어서 할 일** 과
**절대 건드리면 안 되는 것** 을 정리한 것이다.

---

## 0. 이번 단계에서 만든 것

지구 위에 건물이 실제로 들어서고, 사람이 들어와 살고, 세금이 걷히고, 오래된
동네가 재건축으로 고밀도화되는 층을 얹었다. 차량·보행자는 없다(STEP 3.2).

```
지형(생성)  →  도로·지구(학생)  →  건물(시뮬레이션)  →  차량(STEP 3.2)
   tiles           build                bld              (없음)
```

### 새 파일

| 파일 | 역할 |
|---|---|
| `src/sim/simConstants.ts` | 밸런스 조절값 전부. **저장 안 됨 → 마음껏 고쳐도 됨** |
| `src/sim/buildings.ts` | 건물 ID 인코딩, 정원, 건설비, 결정론적 난수 |
| `src/sim/roadGraph.ts` | 도로망 거리장(통근) + 도로 도달 마스크 |
| `src/sim/growth.ts` | 신축·재건축 규칙 |
| `src/sim/macro.ts` | 틱 루프, 수요, 만족도, 입주율, 재정 |
| `src/render/buildingAtlas.ts` | 건물 스프라이트 아틀라스 (+ 임시 그림) |
| `src/render/buildingMesh.ts` | 청크당 건물 1메시 |
| `src/ui/cityPanel.ts` | 돈·인구·날짜·RCI 수요 막대 |
| `tools/simcheck.ts` | 브라우저 없이 시뮬만 돌려보는 검증 스크립트 |

### 고친 파일

`src/world/world.ts`(전면 재작성), `src/world/build.ts`(1줄),
`src/net/types.ts`, `src/net/citySave.ts`, `src/net/saveManager.ts`,
`src/core/constants.ts`, `src/render/worldRenderer.ts`,
`src/ui/tools.ts`, `src/ui/hud.ts`, `src/main.ts`, `index.html`, `src/style.css`

---

## 1. 게임 규칙 (이다온이 확정한 설계)

### 3계층 시민 = 건물 3등급

| 등급 | 계층 | 크기 | 비고 |
|---|---|---|---|
| 1 | 저소득 | 1×1 | |
| 2 | 중산층 | 2×2 | |
| 3 | 고소득 | 3×3 | |

주거·상업·공업 **모두** 같은 3등급 구조를 쓴다. 건물 종류는 9가지다.

### 신축

계층별 수요를 가중치로 등급을 뽑는다. 뽑힌 등급의 부지가 안 나오면 한 단계씩
낮춘다. 그래서 수요가 중산층에 쏠려도 좁은 자투리땅에는 1등급이 들어선다.

### 재건축 — **상향 전용**

- **그 청크에 더 지을 빈 부지가 없을 때만** 발동한다.
- 목표 부지(2×2 / 3×3) 안의 **모든** 건물이 조건을 만족해야 한다.
  - 충분히 오래됨 (`REBUILD_MIN_AGE_DAYS`)
  - 같은 지구
  - 부지 밖으로 삐져나오지 않음
  - 모두 목표 등급보다 낮음
- 하향 재건축은 **없다.**

### 하향이 없는 이유 = 공실

수요가 줄거나 만족도가 떨어지면 **건물은 남고 사람만 떠난다.**
만족도 기준선은 등급이 올라갈수록 높다(`SATISFACTION_FLOOR`). 고급 아파트가
먼저 비는 게 그래서다.

이 규칙이 밸런스의 핵심 장치이기도 하다. 과잉 건설을 하면 → 공실 → 입주율 하락
→ 성장 압력 소멸 → 성장 정지. 건물을 헐지 않고도 과잉 건설이 저절로 벌을 받는다.

### 판정 범위 = 청크(64×64)

이다온이 명시적으로 지정했다. 더 좁게 잡으면 2등급 건물 여덟 채 남짓한 범위에서
재건축이 돌아 청크를 만든 의미가 없고, 나중에 4×4 이상 큰 건물이 들어올 여지도
사라진다.

---

## 2. ChatGPT 가 할 일

### 2-1. 건물 그림 (가장 중요)

**`public/sprites/buildings.png` — 1152 × 384**

지금은 코드가 임시 건물(단순 아이소메트릭 상자)을 그리고 있다. 파일을 넣으면
자동으로 그쪽을 쓴다. 파일이 없어도 게임은 돈다.

```
y   0 ~  63   1등급 밴드   셀  64× 64   (6칸)
y  64 ~ 191   2등급 밴드   셀 128×128   (6칸)
y 192 ~ 383   3등급 밴드   셀 192×192   (6칸)
```

각 밴드의 x 방향 셀 순서:

```
[0] 주거A  [1] 주거B  [2] 상업A  [3] 상업B  [4] 공업A  [5] 공업B
```

셀 안쪽 규칙:

- 셀은 **정사각형**이다. 한 변 = 64 × 등급.
- 셀 아래쪽 절반이 **바닥 다이아몬드**(지면), 위쪽 절반이 건물 몸통이다.
  - 1등급 셀 64×64 → 바닥 다이아몬드는 64×32, 셀의 아래 32px
  - 2등급 셀 128×128 → 바닥 128×64, 셀의 아래 64px
  - 3등급 셀 192×192 → 바닥 192×96, 셀의 아래 96px
- 바닥 다이아몬드의 **아래 꼭짓점 = 셀의 가운데 아래 끝**. 이 점이 화면에
  꽂히는 기준점이라 어긋나면 건물이 땅에서 뜨거나 파묻힌다.
- 배경 투명. 안티에일리어싱 없이(픽셀아트).
- 몸통이 셀 위쪽 절반을 넘어가도 된다(고층). 다만 셀 밖으로는 못 나간다.

만들면 안 되는 것:

- **방향(회전) 변형 금지.** 카메라 회전이 없다. 넣으면 그림이 4배가 된다.
- 변형은 등급·용도당 **2종**. 늘리려면 `BUILDING_VARIANTS` 를 같이 고쳐야 한다.

변형 A/B 는 좌표로 결정론적으로 골라진다(`simHash`). 같은 건물은 다시 그려도
항상 같은 변형이 나오므로 화면이 깜빡이지 않는다.

### 2-2. 밸런스 조정

`src/sim/simConstants.ts` 하나만 보면 된다. 여기 값은 **저장되지 않으므로**
아무리 고쳐도 학생 도시가 안 깨진다.

검증은 브라우저 없이 돌릴 수 있다:

```bash
npx esbuild tools/simcheck.ts --bundle --format=esm --platform=node \
  --outfile=/tmp/simcheck.mjs --alias:pixi.js=/tmp/pixistub.js
node /tmp/simcheck.mjs
```

`/tmp/pixistub.js` 는 Pixi 를 대신하는 빈 껍데기다:

```js
export class Texture { static from(){ return { source:{} }; } }
export class Mesh {}
export class MeshGeometry {}
export class Container {}
export class Graphics {}
export class Application {}
```

봐야 할 것:

- 도시가 계속 자라는가 (인구가 어느 시점에 멈추면 안 된다)
- 입주율이 75~92% 근처에서 도는가 (50% 아래로 떨어지면 과잉 건설)
- 청크가 꽉 찬 뒤 2등급·3등급 재건축이 실제로 일어나는가
- 돈이 파산으로 곤두박질치지 않는가

밸런스가 흔들릴 때 만질 순서:

1. `EXPORT_PER_SQRT_POP` — 도시 성장의 **유일한 외부 동력**. 올리면 도시가 커진다
2. `GROWTH_PRESSURE` / `OCCUPANCY_HEALTHY` — 공실이 성장을 얼마나 세게 막는가
3. `MAX_BUILDS_PER_TICK` / `DEMAND_SMOOTH` — 과잉 건설 속도
4. `REBUILD_MIN_AGE_DAYS` / `REBUILD_DEMAND_GAP` — 재건축이 너무 잦은가/드문가

### 2-3. 문법·타입 확인

```bash
npx tsc --noEmit
npx vite build
```

### 2-4. 남은 마무리

- `tools/simcheck.ts` 가 `dist` 에 안 들어가는지 확인(엔트리에서 참조 안 됨)
- HUD 문구·도시 상태판 문구 다듬기
- 공실을 화면에서 보여줄 방법 (지금은 상태판 숫자로만 보인다)

---

## 3. 절대 건드리면 안 되는 것

### 3-1. 저장에 실린 값

바꾸면 이미 저장된 학생 도시가 어긋난다.

- `src/core/constants.ts` 의 `WORLD_SEED`, `TILE_W/H`, `HEIGHT_UNIT`, `CHUNK_SIZE`
- `src/world/terrain.ts` 의 `hash2`, `valueNoise`, `fbm`
  (Vercel 함수가 **똑같은 값** 을 내야 한다)
- `src/net/codec.ts` 의 RLE 압축 형식
- `src/world/build.ts` 의 `Build` ID 0~3
- `src/sim/buildings.ts` 의 건물 코드 0~8 순서
  - `BLD_NONE = 255` 는 `OVERRIDE_NONE` 과 **같아야 한다**(코덱 재사용)
  - `BLD_COVERED = 254`
  - 새 값은 **9번부터 뒤에만** 붙인다

`SCHEMA_VERSION` 은 올리지 않았다. 이번 변경은 필드를 더하기만 했고
(`bld`, `bornLo`, `bornHi`), 옛 문서에는 그 필드가 없어 `decodeOverride(null)`
이 `null` 을 돌려주므로 "건물 없음" 으로 자연스럽게 읽힌다.

### 3-2. 성능 구조

- **청크 메시 = 드로우콜 1.** 스프라이트 방식으로 되돌리지 말 것.
  건물은 별도 메시라 청크당 2가 된다. 여기까지가 예산이다.
- 도로·지구는 정점을 늘리지 않고 **윗면 UV 만** 바꾼다.
- `World.setBuild` 는 `revision` 을 올리지 않는다. 올리면 드래그 건설 때
  매 프레임 4096칸 UV 를 다시 쓴다.

### 3-3. 시뮬레이션 안에서 금지

- `Math.random()` — 오프라인 따라잡기가 재현되지 않는다. `simRandom` 을 쓴다
- `Date.now()` — 틱 안에서 부르면 결정론이 깨진다

나중에 Vercel 함수가 같은 계산을 재현해 검증할 수 있어야 한다.

---

## 4. 구조에서 꼭 알아야 할 결정 세 가지

### 4-1. 필지(Parcel)를 지형에서 떼어냈다

`world.ts` 를 다시 썼다. 이유:

지형은 화면 밖으로 나가면 버렸다가 다시 만든다(4096칸 노이즈). 그런데 매크로
틱은 **화면에 없는 청크까지** 돌아야 한다 — 도시 반대편 주거지도 계속 자라야
하니까. 건물 데이터가 지형에 붙어 있으면 틱마다 청크를 되살려야 하고 노이즈
계산이 터진다.

```
Chunk   tiles, heights          ← 화면 밖이면 버림
Parcel  build, bld, born, 파생값 ← 절대 안 버림
```

`Chunk.parcel` 로 연결돼 있다. 빈 필지(카메라가 그냥 지나간 자리)는 같이 버린다.

### 4-2. 건물 나이를 저장하지 않는다 — 건설 날짜를 저장한다

나이를 저장하면 매 틱 모든 건물 칸의 값이 바뀌어서 **도시의 모든 청크가 매 틱
저장 대상**이 된다. Spark 무료 한도(하루 쓰기 2만)가 하루 만에 날아간다.

건설 날짜는 한 번 쓰고 다시는 안 바뀐다. 나이는 `지금 날짜 - 건설 날짜`.

같은 이유로 **입주율·만족도·수요·통근거리도 저장하지 않는다.** 전부 건물 배치와
도로 배치에서 다시 계산된다.

### 4-3. 저장 주기를 둘로 나눴다

매크로 틱이 돌기 시작하면 학생이 아무것도 안 해도 도시가 계속 바뀐다.
학생 조작과 같은 60초 주기로 저장하면 20명이 수업 한 번에 Spark 한도를 넘긴다.

| 무엇이 바뀌었나 | 저장 간격 |
|---|---|
| 학생이 직접 고침 (도로·지구) | `SAVE_MIN_INTERVAL_MS` = 60초 |
| 시뮬레이션이 지음 (건물) | `SAVE_SIM_INTERVAL_MS` = 10분 |

어느 쪽이든 탭을 닫거나 로그아웃하면 즉시 저장한다.

---

## 5. 알아둘 규칙 몇 가지

- **건물은 청크를 넘을 수 없다.** 두 저장 문서에 걸치면 반쪽만 저장됐을 때
  반쪽짜리 건물이 남는다. 청크 경계 한두 줄에서 큰 건물이 안 올라가는 건 감수한다.
- **부지는 고도가 전부 같아야 한다.** 아니면 건물이 절벽에 걸친다.
- **도로 접근 반경 `ROAD_REACH = 2`.** 도로에 딱 붙은 칸만 개발하면 블록 안쪽
  땅이 영원히 죽는다. 2면 5칸 폭 블록이 통째로 개발된다.
- **지구를 지우면 그 위 건물도 헐린다.** 지구 없는 땅에 건물만 떠 있으면 안 된다.
- **파산해도 도시를 초기화하지 않는다.** 새 건설만 막히고 유지비는 계속 나간다.
- **토지가치는 STEP 4 로 미뤘다.** 만족도 함수에 항을 더 붙이는 방식으로 들어온다.

---

## 6. STEP 3.2 로 넘어갈 때

`RoadField` 가 이미 통근 흐름의 뼈대다.

- `toJobs` — 각 도로 칸에서 가장 가까운 직장까지의 거리
- `toHomes` — 각 도로 칸에서 가장 가까운 주거지까지의 거리

차량은 이 거리장을 타고 내려가면 된다. 매크로가 만들어낸 통근 수요를 개별 차량
객체로 풀어내고, 그 혼잡도를 다시 만족도로 되먹이는 것이 STEP 3.2 다.

`MacroSim.hourOfDay` 가 이미 있다. 출퇴근 러시를 만들 때 쓴다.
