# 기능 보존 정리 — 2026-09-09

## 시간 제한 해제 후 추가 정리

사용자가 시간 제한 없이 계속 진행하도록 요청한 뒤, 기준 main `16b0b7c`에서 구조와 반복 작업을 추가로 정리했다.

| 영역 | 변경 |
|---|---|
| 진입점 | main에서 도구 모음, HUD 데이터 수집, 타일 설명을 각 UI 모듈로 분리. 아틀라스 4종을 함께 로드 |
| 설정 | 단일 simConstants 파일을 매크로·교통·시설·재난 config로 분리. 기존 export 경로와 모든 값 보존 |
| 매크로 | 도시 통계 타입/초기값과 만족도 계산을 분리. 틱 처리 순서 유지 |
| 렌더링 | 건물·시설 메시 생명주기 공통화, 신호 표시 분리, 빈 시설 청크 검사 결과 캐시 |
| 프레임 연산 | HUD 표시 주기(200ms)에서만 상세 데이터 수집, 차량 차선 좌표 중복 계산 제거 |
| 경로 요청 | Array.shift 반복을 읽기 위치 기반 FIFO로 변경. 소비된 요청 참조를 해제하고 주기적으로 압축 |
| 코드 형식 | 전체 소스·검사 코드 Prettier 통일, TypeScript 분석 기반 import 정리, format/check 명령 추가 |

검증 근거:

- 변경 전 실제 코드와 정점·UV·인덱스 17개 스냅샷이 일치한다.
- 모든 설정 export, 초기 도시 2개, 60일 매크로 통계·자금·수요·사건·배정·저장 타일 결과가 이전 main과 일치한다.
- 구조 검사 20개: 빈 시설 청크 300프레임에서 생성 한 번, 배치/철거/필지 교체/퇴출 무효화, HUD 표시 주기와 지연 계산, 경로 큐 2,301건의 예산·압축·콜백 추가 요청 순서를 확인한다.
- `format:check`, TypeScript/Vite build, 전체 기존 회귀 검사, 변동 프레임 교통 검사 모두 통과했다.
- 기본 프레임 교통 결과는 리팩터링 전 main 코드를 별도로 실행한 로그와 전체가 일치했다. 최대 202대, 적신호 우회전 82회, 정지 위반 0회다. 변동 프레임은 최대 201대, 적신호 우회전 83회, 정지 위반 0회였다.
- 최신 main 기준 220일 성장 검사는 최종 인구 6,538명, 자금 ₩1,493,623, 입주율 77%로 통과했다. 이 수치를 최초 STEP 3.4 보고서와 직접 비교하면 안 된다. 중간 main에 경사 도로·도시 생성 변경이 추가되었으며 이번 정리의 기준은 `16b0b7c`다.
- 실제 브라우저에서 도시 시작, 격자 토글, 시설 메뉴 열기/닫기, 선택 건물의 입주·서비스·복지 설명을 확인했다.

FPS 개선 비율을 측정한 것은 아니다. 확인한 최적화는 불필요한 생성·조회·배열 이동의 제거다. 실계정 Firestore 왕복은 설정이 없어 검증하지 않았으며 저장 로직은 동작 변경 없이 import/형식만 정리했다. 새 파일별 역할과 유지 기준은 [ARCHITECTURE.md](ARCHITECTURE.md)에 정리했다.

## 최초 시간 제한 작업 기록

00:03 목표에 맞춰 렌더링 중복, 매 프레임 메모리 할당, 상태판 갱신, 재난 타입 책임을 우선 정리했다. 전체 아키텍처를 재작성한 작업은 아니다. 교통·성장·서비스의 실행 순서와 밸런스, 저장 형식, 지형 규격은 유지했다.

- `render/quadBuffers.ts`: 건물·시설·차량의 정점/UV 기록과 삼각형 인덱스 생성 공통화. 기존 사각형 순서와 빈 메시 규격을 보존한다.
- 차량마다 프레임당 만들어지던 좌표/UV 임시 배열 두 개를 제거했다. 비활성 차량 슬롯은 TypedArray.fill로 비워 임시 배열 할당 없이 기존 버퍼 내용을 제거한다.
- `sim/disasterTypes.ts`: 저장 계약을 실행 클래스에서 분리했다. 기존 disasters 모듈의 타입 export도 유지해 소비자 호환성을 보존했다.
- 재난 사건이 유효한 상태에서는 목록과 인덱스를 다시 생성하지 않는다. 자연 발생 유예 중이고 사건도 없으면 불필요한 Set/Map 생성을 생략한다.
- 도시 상태판의 사건 표시를 별도 메서드로 분리했다. 값이 같은 텍스트는 DOM에 다시 쓰지 않아 텍스트 노드 교체와 반복 live-region 알림을 줄인다.
- 반복 사용되는 사건 종류/색상 배열을 상수로 옮겼다.

검증: TypeScript/Vite build, 전체 `npm run check`(서비스·교통·차선·220일 성장·재난 35항목·아틀라스), 브라우저 시설·사건 표시를 확인했다. 기존 Pixi 번들 크기 경고는 남는다.

추가 `node tools/check/meshParity.mjs`는 기준 커밋 `c9cda6b`의 실제 렌더러와 현재 렌더러를 같은 입력으로 실행한다. 음수 청크 좌표, 빈/채워진 건물·시설, 움직이는 차량, 차량 수 감소·0대 등 17개 스냅샷의 정점·UV·인덱스가 완전히 일치한다. Git 이력에 기준 커밋이 있어야 실행할 수 있다.

최초 검사는 `6bdf021` 기준으로 완료했다. push 직전에 원격 main의 경사 도로·대도시 생성기 커밋 `c9cda6b`가 발견되어 해당 변경 위에 리베이스했다. 다른 작업을 덮어쓰지 않고 통합·재검증하기 위해 00:03 push 목표는 초과했다.

성능은 할당 및 중복 작업 제거로 확인했으며 FPS 향상률을 측정한 작업은 아니다. 전체 파일의 모든 개선 가능성을 소진했다고 주장하지 않는다.
