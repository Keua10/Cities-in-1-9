# FIX12 — 교통 핵심 재정리

## 핵심 원인
이전 패치는 차량의 실제 시뮬레이션 진행도는 도로 중앙선을 기준으로 두고,
렌더링에서만 옆으로 밀었다. 그래서 직선/코너/충돌 계산이 서로 다른 좌표계를 썼다.

## 변경
- `src/sim/traffic/laneGeometry.ts` 추가
  - 우측 차선을 타일 좌표계에서 한 번만 정의.
  - 직선 노드: 진행방향 오른쪽으로 0.20타일.
  - 회전 노드: 진입 우측차선과 진출 우측차선의 교점을 사용.
- `vehicleMesh.ts`
  - 화면 픽셀 오프셋 제거.
  - 공유 laneGeometry가 주는 실제 차선 좌표를 그대로 렌더링.
- `trafficSim.ts`
  - 신호 교차로뿐 아니라 90도 코너도 conflict reservation 적용.
  - 같은 진입로 최소 700ms 간격.
  - 글로벌 정상 스폰 간격 380~850ms.
  - A* 준비차량에 최대 1.8초 추가 분산.
  - trip 발생 예산을 router 처리량(5/frame)에 맞춤.
- `citizens.ts`
  - 비근무 시민 장보기 시작 경계를 10:00 -> 09:30으로 넓혀 10시 정각 급증 완화.
- `vehicleAtlas.ts`
  - fallback 차량에 전조등/후미등 점을 추가해 진행방향을 테스트 화면에서 확인 가능.

## 유지
- `MAX_ACTIVE_VEHICLES = 1000`
- `MAX_SPAWNS_PER_SEC = 90` (비상 상한)
- daytime 07:30 테스트 시작 / 600초=1일
- 기존 저장 포맷/건물 ID
