# 경사 도로 수정 + 대도시 생성기

작업 날짜 2026-09-08 · 브랜치 `claude/slope-road-bug-fix-0dtztc`

---

## 1. 경사 도로가 "구현 안 된" 진짜 이유

STEP 2 는 비탈 도로의 **규칙**을 만들었다(`build.ts` 의 경사 판정: 한 칸이 여러
방향으로 비탈지면 못 놓는다). 그런데 **비탈을 그리는 코드는 없었다.**

타일 윗면은 언제나 자기 고도의 평평한 마름모 한 장이었고, 고도가 다른 이웃과는
절벽(흙벽)으로 이어졌다. 그래서 언덕을 오르는 도로는 이렇게 나왔다.

```
고도 1 도로 [평평] │흙 절벽 16px│ [평평] 고도 0 도로
```

도로 한복판을 흙벽이 가로지르고, 차는 그 벽을 뚫고 순간이동한다.

### 고친 방식 — 변의 높이는 두 타일의 평균 (`src/world/slope.ts`, 신규)

도로 타일의 윗면을 평면 하나로 본다.

```
맞닿은 두 도로 타일의 고도가 다르면  그 변의 높이 = 두 고도의 평균
그 밖의 변(지형·건물과 닿는 변)      자기 고도 그대로
```

이 규칙 하나로 도로면이 틈 없이 이어진다.

- 고도 0-1 로 맞닿은 두 도로는 양쪽 모두 그 변이 0.5 라 정확히 붙는다.
- 0-1-2 로 이어지는 곧은 비탈길의 가운데 칸은 0.5 -> 1.5 램프가 된다.
- 한쪽만 비탈진 "비탈의 시작" 칸은 중심이 0.25 만 뜨고, 그 덕에 아래 칸과 닿는
  변이 정확히 0.5 로 떨어진다.

평면이므로 타일 하나는 (중심 높이, tx 기울기, ty 기울기) 세 수로 끝난다.
아이소메트릭 투영에서 이 평면은 **정점 4개의 y 만 움직이면** 나온다.
사각형도 드로우콜도 늘지 않는다.

### 같이 고친 것들

| 파일 | 무엇을 |
|---|---|
| `render/chunkMesh.ts` | 윗면 사각형이 지면 평면을 따르게. 절벽은 위·아래 모서리를 꼭짓점마다 잡아 램프에 맞춰 기운다. 램프로 이어지는 변에는 벽을 안 만든다 |
| `render/vehicleMesh.ts` | 차량 높이를 타일 중심 고도 보간이 아니라 **도로면**(`surfaceHeightAt`)에서 읽는다. 바퀴가 노면에 붙는다 |
| `render/worldRenderer.ts` | 커서 마름모도 지면 평면을 따른다. 비탈에 도로를 놓거나 헐면 그 청크 메시를 다시 굽는다(UV 만 고치면 예전 계단이 남는다) |

절벽 계산이 "고도차 = 사각형 장수" 에서 "위·아래 모서리 높이" 로 바뀌었다.
평지끼리 만나면 예전과 **정확히 같은 값**이 나오므로, 도로가 없는 청크의 정점은
한 픽셀도 바뀌지 않는다. 램프 옆 평지에서만 반 칸짜리 흙 쐐기가 새로 생긴다
(그게 없으면 그 자리에 배경이 비쳐 보인다 — 실제로 처음 판에서 그랬다).

---

## 2. 도로에 안 닿은 건물이 비던 문제 (`sim/growth.ts`)

HUD 의 "도로와 연결되지 않은 건물 N채가 비어 있습니다" 의 원인이다.

- 지을 때의 기준: `field.nearRoad` — **반경 2칸 안에** 도로가 있으면 짓는다
- 통근 거리의 기준: `roadGraph.commuteFor` — 부지에 **맞닿은** 도로에서만 잰다

두 기준이 어긋나 있어서, 도로에서 두 칸 떨어진 자리에 건물이 서고는 통근 거리가
무한대라 영원히 비었다. 큰 도시에서는 그런 건물이 수천 채까지 늘어난다
(실측: 생성 도시 30일 주행에서 2,172채).

지을 때의 기준을 통근의 기준에 맞췄다. "지구를 도로에서 두 칸 넘게 칠하면
가운데가 안 자란다" 는 STEP 3.1 의 설계 의도는 그대로고, 오히려 이제야 지켜진다.

---

## 3. 테스트용 대도시 생성기 (`src/world/cityGen.ts`, 신규 · `testCity.ts` 삭제)

예전 `testCity.ts` 는 20x20 에 3x3 격자였다. 시뮬레이션이 도는지 보기에는
충분했지만 경사 도로도, 구역 성격도, 교차로 밀도 차이도 없었다.

새 생성기는 도시가 자라는 순서를 따라간다.

1. 지형을 읽고 **도심 자리**를 고른다 (넓고 평평하고 영역 한가운데에 가까운 곳)
2. 도심 둘레에 **구역 씨앗**을 뿌린다 — 안쪽 고리에 부도심·주거, 바깥 고리에
   신도시 주거와 공업지대. 구역 경계는 잡음을 곱해 구불구불하다
3. 씨앗끼리 **간선도로**로 잇는다. A* 가 물을 피하고 고도 변화를 비싸게 치므로
   길이 등고선을 따라 휘고, 언덕은 넘어야 할 때만 넘는다 — 그 자리가 경사 도로다
4. 구역마다 **자기 결의 격자**로 이면도로. 블록 크기·방향·위상이 구역마다 다르고,
   선은 구역 안에 있는 동안만 그어지며 가끔 한 칸 어긋나거나 막다른 길이 된다
5. 비탈 규칙을 못 지키는 도로와 도심에서 끊긴 도로를 걷어낸다
6. 시설 -> 지구 -> 건물 순으로 채운다

### 두 가지 수치가 설계를 좌우한다

**지구는 도로에서 두 칸까지만 칠한다.** 세 칸 넘게 칠하면 안쪽이 도로에 못 닿아
2번 문제가 그대로 재발한다. 안 칠한 블록 속살은 마당·공터로 남고, 위에서 보면
도시가 훨씬 자연스럽다.

**시설은 커버리지 격자로 놓는다.** 구역마다 한두 채씩 놓은 첫 판은 입주율이
28% 였다. 이 게임의 만족도는 서비스·복지 부족이 0.5까지 깎고 2·3단계 건물의
입주 기준선이 0.45 / 0.62 라, 커버가 모자라면 **큰 도시일수록 공실률이 오른다.**
간격을 정원과 반경에서 거꾸로 잡았다(소방 24칸, 경찰 46, 병원 62, 학교 42,
공원 16, 소공원 11칸마다). 입주율 28% -> 69% 가 됐다.

### 도시 크기는 취향이 아니라 예산이다 (`CITY_RADIUS = 70`)

매크로 시뮬레이션은 하루에 한 번(60초마다) 통근 배정과 혼잡 추정을 다시 만드는데,
둘 다 "집·직장마다 도로망 BFS" 라 비용이 도시 크기에 비례한다.
처음 만든 반경 125짜리 도시(건물 6,700채)는 그 재계산에 **6.5초**가 걸렸다.

반경 70 + 아래 4번의 최적화로 하루치 재계산이 한 틱 0.4초 안쪽이 됐다.

---

## 4. 시뮬레이션 재계산 최적화

| 무엇 | 전 | 후 |
|---|---|---|
| 거리장 맵 키를 `"x,y"` 문자열 -> 정수 하나로 (`roadGraph.tileKey`) | 혼잡 추정 2,200ms | 390ms |
| 혼잡 추정 BFS 상한을 200 -> 통근·쇼핑 반경(45) | | 포함 |
| 배정표에서 반경 밖 직장을 **객체 만들기 전에** 거르기 | 배정 660ms | 380ms |
| 거리장 캐시(집 진입 도로 24개 / 목적지 256개, FIFO) | | 포함 |
| 배정·혼잡을 도로망 재계산과 **다른 틱**으로 미루기 | 한 틱 6.5초 | 한 틱 0.4초 |

셋을 한 틱에 몰아 돌 이유가 없다. 틱 간격이 2.5초이고 하루에 한 번 하는 일이라
한두 틱 늦어도 결과가 달라지지 않는다.

---

## 5. "맵 초기화" 버튼

이미 저장된 도시가 있으면 생성기가 절대 손대지 않으므로(그래야 학생 도시를
안 날린다), 새 도시를 보려면 지우는 길이 필요하다. 툴바 맨 오른쪽에 추가했다.

```
world.clearBuilt()      필지 배열만 비운다(키는 남긴다 -> 저장 때 빈 청크로 나가
                        citySave 가 그 문서를 지운다. 필지를 지우면 서버에 예전
                        도시가 남아 새로고침할 때 되살아난다)
sim.resetState()        돈·틱·재해 초기화
saver.saveNow()         빈 상태를 서버에 확정
location.reload()       새로 뜬 페이지가 빈 도시를 보고 새 대도시를 만든다
```

메시·도로망·경로·입주율 캐시를 하나하나 무효화하는 것보다 새로고침이 확실하다.
로그인 안 한 상태에서도 생성이 결정론적이라 같은 도시가 다시 나온다.

---

## 6. 실측 (반경 70, 도시 0번)

```
생성 207ms · 접속 시 첫 계산 840ms · 하루 24틱 1.4초 · 최악 한 틱 396ms
도로 3,488칸 (그중 경사 구간 361칸) · 시설 188채
초기 건물 2,323채 -> 30일 뒤 3,340채
인구 20,000명 · 입주율 69% · 고립 건물 0채
수입 49,800/일 · 지출 17,000/일
```

---

## 7. 남은 것 / 주의

- `npm run check` 는 이번 작업에서 돌리지 않았다(요청). `growth.ts` 의 도로 접함
  기준이 바뀌었으므로 `tools/simcheck.ts` 처럼 도로에서 두 칸 떨어진 지구가
  자라기를 기대하는 검사가 있다면 기대값을 함께 고쳐야 한다.
- 램프는 고도차 한 단계만 잇는다. 두 단계 이상 차이 나는 자리(지형 편집으로만
  생긴다)는 예전처럼 절벽으로 둔다 — 한 칸에 두 단계 램프를 그리면 도로가 벽이 된다.
- 경사 타일 위의 건물·시설은 여전히 평평한 땅에만 선다(설계 그대로).
