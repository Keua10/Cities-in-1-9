# STEP 3 수정 패치 완료 기록

기준: GitHub `main` + 기존 STEP2/STEP3 패치

## 1. 초기 안개 범위
- `BASE_CHUNK_SPAN = 2`인 2x2 베이스를 기준으로 바깥 한 청크까지 처음부터 개척 처리.
- 초기 가시 영역은 정확히 4x4 청크: `baseCx-1..baseCx+2`, `baseCy-1..baseCy+2`.
- 상하좌우뿐 아니라 대각선 청크도 포함.
- 그 밖의 청크는 기존 `WorldRenderer`의 `isExplored()` 안개 판정을 그대로 사용.

## 2. 베이스 후보 지형 품질 검사
- 베이스 후보 2x2 영역을 8타일 간격으로 샘플링.
- 물: `WaterDeep`, `WaterShallow`.
- 산: 고도 5 이상.
- 평지: 물이 아니고 고도 5 미만.
- `평지 / (물 + 산) > 1`인 후보만 채택. 즉 평지가 과반을 넘지 못하면 다음 후보를 탐색.
- 기존 STEP2 규칙 유지:
  - 일반적으로 베이스 간 8청크 이상.
  - 두 베이스 사이를 산 장벽이 가로막으면 4청크 이상으로 완화.
  - 어떤 경우에도 4청크 미만 금지.
- 모든 판단은 기존 `WORLD_SEED` 기반 지형 함수로 이루어져 결정론 유지.

## 3. 대도시 생성 도로의 명시적 연결
- 기존 대도시 생성 도로가 `roadLinks=255`로 생성되어 맞닿은 도로가 전부 자동 연결되던 동작을 생성 시점에만 차단.
- 생성 도로는 명시적 연결값 0에서 시작.
- 생성 완료 후 실제 도로 형태를 보고 필요한 인접 방향만 연결.
- 2칸 폭 큰길의 평행 차선끼리는 매 타일마다 횡방향 연결하지 않음.
- 실제 교차/T자/합류 형태에서는 연결 허용.
- 연결 시 기존 `canConnectRoads()` 검사를 거치므로 경사 연결 규칙도 유지.
- `World`의 기존 저장 호환 규칙과 학생이 직접 만드는 도로 동작은 변경하지 않음.

## 검사
- 수정 파일 TypeScript 구문 검사: 오류 0.
- `spawn.ts`, `citySeed.ts` strict 타입 검사(현재 인터페이스 기준): 오류 0.
- 실제 현재 지형 생성식으로 베이스 100개 계산:
  - 지형 비율 위반 0.
  - 최소 베이스 거리 4청크.
  - 8청크 미만 쌍 36개 모두 산 장벽 조건 통과.
  - 계산 시간 약 39ms(검사 환경 기준).
- 안개 초기 범위 계산: 정확히 16청크(4x4).
- 2칸 폭 도로 모의 검사:
  - 수평/수직 각 차선의 진행 방향 연결 유지.
  - 평행 차선 사이 불필요한 횡연결 0.
  - 실제 교차부 연결 생성 확인.

## 적용 시 주의
베이스 좌표 선택 알고리즘 자체가 바뀌므로 기존 저장 도시와 좌표가 달라질 수 있다. 이 패치는 새 맵/맵 초기화 후 확인하는 것을 전제로 한다.
