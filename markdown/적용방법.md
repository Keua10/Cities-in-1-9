# FIX14 적용 방법

이 압축 파일 안의 폴더 구조는 저장소 루트 그대로다. **압축을 저장소 루트에 풀고
덮어쓰기만 하면 끝이다.** 삭제해야 하는 파일은 하나도 없다.

## 1. 적용

```bash
cd Cities-in-1-9          # 저장소 루트 (package.json 이 있는 곳)
git checkout main
git pull

# 이 zip 을 저장소 루트에 풀고 덮어쓰기
unzip -o ~/Downloads/FIX14-traffic-rules-files.zip -d .

# 이 안내문은 저장소에 올릴 필요가 없다
rm -f 적용방법.md

git add -A
git commit -m "FIX14: 교차로 인식·통행 우선순위·겹침 제거"
git push
```

Windows 탐색기로 할 때는 zip 안의 `src`, `tools` 폴더와
`FIX14_TRAFFIC_RULES_NOTES.md` 를 저장소 폴더에 그대로 끌어다 놓고
"덮어쓰기" 를 고르면 된다. `적용방법.md` 는 빼고 넣는다.

## 2. 바뀌는 파일 (15개)

새로 생기는 파일 (4개)

```
src/sim/traffic/junctions.ts             교차로 영역 검출
src/sim/traffic/intersectionControl.ts   통행권 예약과 우선순위 배분
src/sim/traffic/collision.ts             차체 겹침 판정 + 공간 해시
tools/check/trafficCheck.ts              교통 규칙 검증기 (개발용)
```

고쳐지는 파일 (10개)

```
src/sim/traffic/trafficSim.ts     이동 루프 재작성
src/sim/traffic/signals.ts        타일별 위상 -> 교차로 영역별 위상
src/sim/traffic/laneGeometry.ts   궤적 충돌 판정 (꺾은선 + 차 폭 기준)
src/sim/traffic/vehicles.ts       차량 상태 필드 추가
src/sim/traffic/router.ts         신호 대기 비용, 차선 변경 비용
src/sim/simConstants.ts           새 조절값
src/sim/congestion.ts             교차로 색인 연결
src/sim/roadGraph.ts              도로 용량 계산이 넓은 도로를 깎지 않도록
src/render/worldRenderer.ts       신호등을 교차로 영역별로 그림
tools/check/laneCheck.ts          바뀐 판정에 맞춰 기대값 수정
```

문서 (1개)

```
FIX14_TRAFFIC_RULES_NOTES.md      무엇을 왜 고쳤는지
```

저장 포맷, 건물 ID, Firestore 관련 파일은 **하나도 건드리지 않았다.**
이미 저장된 도시가 그대로 열린다.

## 3. 적용 후 확인

```bash
npm install          # 아직 안 했다면
npm run build        # tsc --noEmit + vite build
```

교통 규칙 검증기를 돌리려면 (선택)

```bash
npx esbuild tools/check/trafficCheck.ts --bundle --platform=node --format=esm \
  --alias:pixi.js=./tools/check/stub-pixi.ts --outfile=/tmp/trafficCheck.mjs
node /tmp/trafficCheck.mjs
```

34개 항목이 전부 OK 로 나오고 마지막에 `교통 규칙 검증 통과` 가 찍히면 정상이다.
`npm run dev` 로 띄워서 4차로를 그어 보면 직선 구간에는 신호등이 없고,
교차로에서만 정지선 앞에 줄을 선다.

## 4. 화면에서 확인할 것

- 4차로 직선 구간 한가운데에 멈춰 선 차가 **없다** (신호등이 거기 없다)
- 신호등은 진짜 교차로에만, 진입로마다 한 등씩 붙는다
- 교차로에서 궤적이 겹치는 차는 동시에 들어가지 않는다 — 직진이 먼저 가고
  좌회전이 뒤에 간다
- 적신호에 우회전 차량이 정지선에서 잠깐 멈췄다가 지나간다
- 가끔 한 대가 무리하게 교차로에 걸쳐 서서 몇 초간 막는다 (꼬리물기 — 의도된
  동작이다). 그 차를 다른 차가 **뚫고 지나가지 않는다.**
