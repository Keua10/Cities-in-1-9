# FIX14 — 교차로 인식·통행 우선순위·겹침 제거

교통이 도시건설의 핵심 콘텐츠라는 전제로, 4차로에서 드러난 문제를 뿌리부터 다시 만들었다.

## 1. 근본 원인 — "이웃 도로가 3개면 교차로"

예전 판정은 이것 하나였다.

```ts
// signals.ts (구)
export function hasSignal(world, tx, ty) {
  const m = roadMask(world, tx, ty);
  return popcount(m) >= 3;
}
```

폭이 2타일인 도로(=4차로)를 그으면 **직선 구간의 모든 타일**이 이웃 3개를 갖는다.

```
...  R R R R R R ...   <- y=0 : 좌/우/아래가 전부 도로 -> "교차로"
...  R R R R R R ...   <- y=1 : 좌/우/위가 전부 도로  -> "교차로"
```

그래서 4차로를 깐 순간 **도로 전체가 신호등 있는 교차로**가 됐다. 화면에서 본
"4차로 한가운데 멈춰 있는 차"는 그 자리에서 빨간불을 만난 차였다. 같은 원인으로

- 경로탐색은 넓은 도로의 모든 타일에 신호 대기 비용을 매겨 4차로를 기피했고,
- 혼잡도 계산은 넓은 도로의 모든 타일 용량을 0.6으로 깎았고,
- 교차로 예약이 도로 전 구간에서 걸려 통행량이 떨어졌다.

그리고 정지선 값이 `INTERSECTION_STOP_T = 0.94` 였다. 진행도 1.0이 곧 교차로
칸의 **중심**이므로, 0.94에서 멈추면 차는 이미 교차로 안에 반 넘게 들어가 있다.
이것도 "교차로 한가운데 서 있는 차"의 원인 중 하나였다.

## 2. 폭에 의존하지 않는 교차로 검출 (`src/sim/traffic/junctions.ts`, 신규)

타일 t 에 대해 가로/세로 최대 연속 도로 구간 `hRun(t)`, `vRun(t)` 를 잡고

```
세로로 갈라진다 := |vRun(t)| > min{ |vRun(s)| : s ∈ hRun(t) }
가로로 갈라진다 := |hRun(t)| > min{ |hRun(s)| : s ∈ vRun(t) }
```

`min` 이 곧 그 도로의 **폭**이다. 폭 2짜리 가로도로의 직선 구간은 모든 타일의
vRun 이 2 라서 min 도 2 이고, 따라서 갈라지지 않는다. 세로도로와 만나는 타일만
vRun 이 폭보다 길어진다. 두 조건이 모두 참인 타일이 교차로 칸이고, 4방향으로
이어 붙이면 **교차로 영역(Junction)** 이 된다.

- 도로 폭을 상수로 박지 않으므로 1차로든 6차로든 그대로 동작한다.
- 4차로 x 4차로 교차로는 2x2 = 4칸짜리 영역 하나가 된다.
- 신호등은 "양쪽 축에서 오는 **길이 2타일 이상의** 진입로가 3갈래 이상" 일 때만
  선다. L자 코너(2갈래)와 도로 옆에 한 칸 튀어나온 진입로에는 서지 않는다.

정지선도 고쳤다. 교차로 칸의 경계는 진행도 0.5, 차체 절반이 0.25이므로
`INTERSECTION_STOP_T = 0.20` 이면 앞 범퍼가 경계선에 딱 선다.

## 3. 신호 — 영역 단위 + 전적색 (`signals.ts` 재작성)

타일마다 따로 돌던 위상을 교차로 영역 하나가 갖는다. 주기는

```
[축0 녹색 7.0s][축0 황색 1.4s][전적색 1.2s][축1 녹색][축1 황색][전적색]
```

**전적색(all-red)** 구간이 핵심이다. 황색이 끝나자마자 반대 축을 녹색으로 바꾸면
황색 끝에 진입한 차와 막 출발한 차가 교차로 한가운데서 만난다. 실제 신호기와
같은 이유로 넣었다.

황색은 "무조건 정지"가 아니라 **딜레마 존**으로 다룬다. 지금 제동해도 정지선을
넘길 상황이면 그대로 통과한다. 그러지 않으면 신호가 바뀔 때마다 급정거가 나온다.

## 4. 통행 우선순위 — 지속 예약 + 점수 배분 (`intersectionControl.ts`, 신규)

예전에는 매 프레임 "이 타일을 쓰는 움직임" 목록을 새로 만들고 다음 타일이 겹치면
멈췄다. 세 가지를 못 했다.

1. **우선순위가 없었다.** 두 차가 동시에 요청하면 배열 순서가 승자를 정했고, 그
   순서는 매 프레임 바뀌었다. 서로 양보하다 둘 다 못 가거나, 둘 다 가서 겹쳤다.
2. **예약이 유지되지 않았다.** 교차로에 들어간 차가 다음 프레임에 통행권을 잃을
   수 있었다. 교차로 한복판에서 궤적이 겹친 직접 원인이다.
3. **타일 단위였다.** 4차로 교차로는 4칸이다. 칸마다 따로 판정하면 서로 다른 칸을
   지나는 두 궤적이 실제로는 스치는데도 각자 통과해 버린다.

지금은 통행권이 **교차로 영역 전체에 대한 지속 예약**이다. 한 번 받으면 다
빠져나갈 때까지 유지된다. 매 프레임 대기 차량을 모아 점수순으로 한 번에 배분하고,
이미 예약된 궤적과 실제로 겹치는 요청만 거절한다.

```
점수 = 신호(녹색 1000 / 황색-딜레마존 950 / 적신호 우회전 200)
     + 회전종류(직진 60 > 우회전 45 > 좌회전 10)
     + 큰길 우선 70            (비신호 교차로)
     - 우측차량 우선 120       (비신호 교차로, 내 오른쪽에서 오는 차가 있으면 양보)
     + 대기시간 가산(최대 380) (굶주림 방지)
     + 결정적 타이브레이크     (프레임마다 승자가 바뀌지 않게)
```

4방향이 동시에 물리면 모두 같은 감점을 받아 대기시간으로 풀린다 — 교착이 없다.

## 5. 실제 법규대로 회전

- **적신호 우회전**: 정지선에서 `RED_RIGHT_STOP_DWELL_MS = 700ms` **일시정지**한
  뒤, 녹색을 받은 축에서 다가오는 차와 궤적이 겹치지 않으면 진행. 적신호 직진은
  아무리 오래 서 있어도 진행 불가.
- **비보호 좌회전**: 마주 오는 직진/우회전이 5타일 안에서 다가오면 양보한다.
  마주 오는 차가 없으면 통과.
- **직진 우선**: 같은 조건이면 직진 > 우회전 > 좌회전.
- **큰길 우선 / 우측차량 우선**: 신호 없는 교차로(L자 코너, 이면도로 T자)에서.
- **보행자**: 아직 구현하지 않았으므로 `pedestrianBlocking()` 이 항상 false 다.
  보행 신호가 들어오면 이 함수만 바꾸면 되고, 적신호 우회전 판정부는 그대로 둔다.

## 6. 궤적 충돌 판정을 다시 만들었다

예전 `movementsConflict` 는 두 궤적의 **직선(현)이 교차하는지**만 봤다. 두 가지가
틀렸다.

- **차 폭을 무시했다.** 선분이 교차하지 않아도 0.1타일 옆을 스치면 차체는 겹친다.
- **회전이 어느 쪽으로 부풀어 도는지를 버렸다.** L자 코너의 양방향은 곡선이 서로
  반대쪽으로 부풀어 실제로는 0.48타일 떨어져 지나가는데, 현으로 재면 0.35타일이라
  서로 막게 된다. 반대로 마주 보는 좌회전끼리는 곡선이 가운데로 파고들어 실제
  0.21타일까지 붙는데(차 폭 0.30 — 실제로 겹친다) 현으로 재면 0.35타일이라
  "지나가도 된다" 는 잘못된 답이 나온다. FIX13 이 "마주 오는 좌회전끼리는 통과"
  라고 적은 것은 이 착시였다.

지금은 칸마다 **시작점 - 베지에 중점 - 끝점** 세 점의 꺾은선을 **타일 절대 좌표**로
만들고, 두 궤적의 최단거리가 `MOVEMENT_CLEARANCE_TILES = 0.40` 보다 가까우면 충돌로
본다. 절대 좌표라 칸이 달라도(넓은 교차로) 판정이 맞고, 중점 하나로 곡선의 방향이
살아난다.

| 상황 | FIX13 | 지금 | 근거 |
| --- | --- | --- | --- |
| 마주 오는 직진 | 통과 | 통과 | 중심 간격 0.50 |
| L자 코너 양방향 | 통과 | 통과 | 실제 곡선 간격 0.48 |
| 마주 보는 좌회전 | 통과 | **충돌** | 실제 곡선 간격 0.21 < 차 폭 0.30 |
| 직교 직진 | 충돌 | 충돌 | 교차 |
| 좌회전 vs 마주 오는 직진 | 충돌 | 충돌 | 교차 |
| 같은 차선 합류 | 충돌 | 충돌 | 진출 방향 동일 |

## 7. 겹침을 구조적으로 없앴다

세 겹으로 막는다.

1. **통행권**: 겹치는 궤적은 애초에 동시에 교차로에 못 들어간다.
2. **앞차 간격**: 경로를 따라 재는 1차원 간격(코너에서도 정확)에 더해,
   차선 소속과 무관하게 "실제로 내 앞을 막고 있는 차"까지의 거리를 본다.
   예전에는 같은 경로 차선만 봐서 옆에서 끼어드는 차, 교차로에 갇힌 차,
   합류 지점에서 만나는 차를 놓쳤다 — 그 셋이 "차가 차를 뚫고 지나가는" 원인이었다.
3. **겹침 방지 장치**(`collision.ts`, 신규): 매 프레임 이동을 먼저 계산만 하고,
   차체 사각형(OBB, 분리축 정리)이 겹치면 그 이동을 취소한다. 프레임 시작 상태가
   겹치지 않는다는 불변식이 유지되므로 **어떤 경로로도 두 차가 포개질 수 없다.**

원 대신 사각형을 쓴 이유: 마주 오는 두 차의 중심 간격은 0.5타일인데 차체 길이가
0.5라서, 반지름으로 판정하면 스쳐 지나갈 때마다 서로를 멈춰 세운다. 실제로 겹치는
것은 차체 사각형이 겹칠 때뿐이다.

## 8. 꼬리물기 — 유지하되 관통 버그는 제거

요청대로 **기본은 금지, 일부 난폭운전만 허용**이다.

- 모든 차량은 만들어질 때 `aggressive` 성향을 한 번 정한다
  (`AGGRESSIVE_DRIVER_PERCENT = 12`).
- 일반 차량은 교차로를 나간 자리에 한 대분 공간(`EXIT_ROOM_TILES = 1.05`)이 없으면
  진입하지 않는다. 난폭운전 차량만 `AGGRESSIVE_EXIT_ROOM_TILES = 0.6` 까지 밀고
  들어간다 — 가끔 한 대가 교차로에 걸쳐 서서 몇 초간 교차로를 막는다.
- 이 판정은 **정지선을 넘는 매 프레임 다시** 한다. 진입 허가를 받을 때 한 번만
  확인하면 허가와 진입 사이에 앞 대기열이 늘어나 그대로 갇히고, 그렇게 갇힌 차
  몇 대가 서로의 교차로를 막아 도시 전체가 영원히 멈춘다(개발 중 실제로 그랬다).
- 갇힌 차는 이제 다른 차를 **뚫고 지나갈 수 없다**(7번의 3단계). 교차로가 막히면
  정말로 막힌 것처럼 보인다.
- 한 교차로가 `GRIDLOCK_RELIEF_MS = 4초` 이상 아무도 못 지나가면 그동안은 난폭운전
  진입도 금지한다. 꼬리물기가 원인인 정체를 꼬리물기로 더 키우지 않는다.

## 9. 그 밖에 같이 고친 것

- **경로 재탐색이 차를 반대 차선으로 순간이동시켰다.** 차선 위치는 "진행방향의
  오른쪽"으로 정해지므로, 새 경로의 첫 방향이 다르면 차가 그 자리에서 도로 건너편
  차선으로 튄다. 하필 그 자리에 마주 오는 차가 있으면 두 차체가 포개진 채 굳고,
  그 한 대가 뒤쪽 대기열 전체를 세운다. 지금은 방향이 같을 때만 경로를 갈아끼운다.
- **차선 변경 비용**(`LANE_CHANGE_COST = 60`). A* 가 4차로에서 한 칸 이득을 보려고
  지그재그로 차선을 갈아타는 경로를 만들었다. 화면에서는 차가 이유 없이 좌우로
  흔들리고, 그 궤적이 마주 오는 차선을 가로질렀다. 이제 정말 필요할 때만 바꾼다.
- **차선 변경 전 목표 차선 확인**. 비어 있어야 넘어간다.
- **혼잡도 용량**이 넓은 도로를 깎지 않는다(`roadTileCapacity` 가 교차로 색인을 본다).
- **신호 대기 비용**은 교차로에 들어갈 때 한 번만 붙는다.
- **차량 생성 판정**이 실제 차선 좌표와 차체 사각형을 본다. 예전에는 타일 번호와
  진행도만 비교해서, 차선이 다른데 막히거나 차체가 겹치는 자리에 차가 생겼다.
- **신호등 렌더링**이 교차로 영역마다 진입로별로 한 등씩 그린다. 예전에는 이웃
  도로가 3개 이상인 타일마다 찍어서 4차로 전체에 신호등이 깔려 보였다.
- **안전밸브**: 겹침 방지 장치에 1.2초 이상 붙잡히면 경로를 조금 물러나 빠져나오고
  (`FREEZE_BREAK_MS`), 3분 동안 한 뼘도 못 간 차는 통행을 포기한다
  (`STUCK_GIVEUP_MS`). 정상 주행에서는 걸리지 않는 길이다.

## 10. 검증

`tools/check/trafficCheck.ts` (신규). 브라우저 없이 돌린다.

```
npx esbuild tools/check/trafficCheck.ts --bundle --platform=node --format=esm \
  --alias:pixi.js=./tools/check/stub-pixi.ts --outfile=/tmp/trafficCheck.mjs
node /tmp/trafficCheck.mjs
```

34개 항목이 통과한다. 중요한 것만:

- 4차로 직선 구간이 교차로로 잡히지 않는다 / 4차로 교차로는 2x2 한 영역이다
- T자·L자·차고지 진입을 구분한다 (신호등이 서는 곳과 서지 않는 곳)
- 직교하는 두 축이 동시에 녹색이 되지 않는다 / 전적색 구간이 있다
- 4차로에서 칸이 달라도 직교 직진은 충돌로 잡힌다
- **4차로 격자 도시를 200초 실제 주행**: 차체가 한 번도 겹치지 않는다,
  통행권 없이 적신호에 진입하는 차가 없다, 교차하는 궤적이 교차로 안에 동시에
  존재하지 않는다, 교착 없이 계속 흐른다
- 적신호 우회전은 일시정지 뒤에만 / 적신호 직진은 불가 / 비보호 좌회전은 양보

`tools/simcheck.ts`(매크로)와 `npm run build`(tsc + vite)도 그대로 통과한다.

## 유지

- 저장 포맷, 건물 ID, `MAX_ACTIVE_VEHICLES = 1000`
- 우측통행 규칙 `오른쪽 = (dir + 1) & 3` (FIX11~13에서 확인한 대로 맞다)
- daytime 600초 = 1일
