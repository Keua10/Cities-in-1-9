# Cities in 1-9 — STEP 4 진입 전 3건 완료 요약

기준 저장소: `Keua10/Cities-in-1-9`의 현재 `main`

## 단계 1 — 도로 설치 메커니즘 + 보행자 이동

현재 `main`에 이미 반영된 상태를 기준으로 유지했다.

- 클릭으로 인접 도로를 새로 놓아도 자동 연결하지 않는다.
- 드래그한 방향의 도로만 명시적으로 연결된다.
- 논리적으로 연결되지 않은 인접 도로는 차량 경로/교차로에서도 별개 도로로 취급한다.
- 보행자는 별도 인도 타일 없이 도로 가장자리와 건물 사이 공간을 따라 이동한다.

## 단계 2 — 베이스 간격 규칙

`src/world/spawn.ts`에서 결정론적인 후보 배치를 사용한다.

- 기본 최소 중심 간격: 8청크
- 산이 두 베이스 중심 사이를 실제로 가로막는 경우: 4청크까지 완화
- 어떤 경우에도 4청크 미만은 허용하지 않는다.
- 새 난수는 사용하지 않고 기존 지형의 `heightAt()`만 사용한다.

산 판정 수치가 요구사항에 정해져 있지 않아 다음 기준을 사용했다.

- 고도 5 이상
- 두 베이스 중심 직선의 가운데 50% 구간을 검사
- 24타일 이상 연속된 고지대가 있어야 산 장벽으로 인정

## 단계 3 — 기본청크 2×2 + 대도시 형성 영역 축소

### 기본청크

`src/core/constants.ts`의 `BASE_CHUNK_SPAN`을 `4`에서 `2`로 변경했다.

- 기존: 4×4청크 = 256×256타일
- 변경: 2×2청크 = 128×128타일

`World`의 초기 개척 영역, `spawn.ts`의 베이스 중심 계산 등은 모두 이 상수를 참조하므로 같은 기준으로 2×2에 맞춰진다.

### "대도시 형성"이 실제로 가리키는 시스템

코드 확인 결과 대상은 `src/world/cityGen.ts`의 **테스트용 대도시 생성기**다.

이 생성기는 별도 3×3 고정 크기를 쓰는 것이 아니라 다음 식으로 생성 영역을 잡는다.

```ts
const SPAN = BASE_CHUNK_SPAN * CHUNK_SIZE;
```

따라서 `BASE_CHUNK_SPAN = 2` 적용만으로 대도시 생성 영역도 자동으로 256×256타일에서 128×128타일로 줄어든다. 내부 도로·지구·시설·건물 생성은 전부 `SPAN` 경계 안에서 실행되므로 별도 크기 상수를 추가하거나 생성 알고리즘을 바꾸지 않았다.

`CITY_RADIUS`는 도시 내부 구역 씨앗과 밀도 분포에 쓰이는 값이므로, 영역 자체가 이미 2×2로 제한되는 상태에서 별도로 줄이면 도시 구조까지 이중 축소될 수 있어 그대로 유지했다.

## 건드리지 않은 것

- `WORLD_SEED`
- `TILE_W`, `TILE_H`
- `HEIGHT_UNIT`
- `CHUNK_SIZE`
- 지형 노이즈 함수 (`hash2`, `valueNoise`, `fbm`)
- 우측통행/교차로 통행권/충돌 방지 규칙
- `MAX_ACTIVE_VEHICLES = 1000`
- `SIM_RADIUS_CHUNKS = 1`
- 청크 렌더링 구조

## 적용 전제

단계 2·3 요구사항대로 **맵 전체 재초기화 후 확인**하는 것을 전제로 한다.
