# STEP 3.4 — 화재·범죄·질병 (2026-09-08)

별도 원본 기획 없이 STEP 3.3 인계서의 서비스 품질 연동을 기준으로 구현한 첫 버전이다. 아래 수치는 이번 구현에서 정한 초기 밸런스이며 플레이 후 조정할 수 있다.

## 동작

- 주거·상업·공업 건물에 화재, 범죄, 질병이 발생한다. 시설은 사건 대상이 아니다.
- 화재 건물의 입주율은 0이다. 소방 품질에 따라 진압되며, 12시간까지 진압되지 않으면 기존 철거 경로로 전소된다. 지구 지정은 남아 다시 성장할 수 있다.
- 화재는 건물 footprint의 변에 바로 닿은 건물로만 확산한다. 청크 경계는 넘어가지만 도로·빈 땅·시설은 건너뛰지 않는다. 새 화재는 다음 틱부터 확산한다.
- 범죄·질병은 각각 만족도를 0.08, 0.12 낮추고 기존 입주율 계산에 반영된다. 경찰·병원이 회복을 빠르게 하며 최대 12/24시간 뒤 해소된다.
- 사건 중인 건물이 포함된 footprint는 상향 재개발할 수 없다. 수동 철거 후 사건은 다음 틱에 정리된다.
- 도시 패널에 세 사건의 현재 수, 대응 안내, 누적 진압·전소 수를 표시한다. ‘사건 위치 보기’로 현재 사건을 순서대로 찾는다. 지도 위에는 불꽃/느낌표/십자 마커, 타일 HUD에는 종류와 경과 시간을 표시한다.

## 확률과 서비스

1틱은 게임 내 1시간이다. 자연 발생 확률은 건물 한 채당 아래 기본값에 `graceFactor(population) * (1 - quality * prevention)`을 곱한다. 인구 400 이하에서는 자연 발생을 유예하고 2,000명에서 전체 확률이 적용된다. 기존 사건은 인구나 자금이 줄어도 계속 처리한다.

| 종류 | 시간당 기본 발생 확률 | 품질 1 예방 비율 | 시간당 기본 회복 | 품질 1 추가 회복 | 최대 시간 |
|---|---:|---:|---:|---:|---:|
| 화재 | 0.000012 | 85% | 0.002 | 0.40 | 12 |
| 범죄 | 0.000030 | 90% | 0.040 | 0.30 | 12 |
| 질병 | 0.000025 | 85% | 0.020 | 0.22 | 24 |

화재 확산 확률은 `0.04 * (1 - sourceQuality) * (1 - targetQuality)`다. 같은 대상에 여러 화재가 접해도 대상·틱 기준 난수를 사용하며 한 번만 발화한다. 최대 활성 사건은 도시당 128건이다.

서비스 입력은 기존 `ServiceField.serviceQualityAt(tx, ty, kind)`만 사용한다. 따라서 도로 거리, 도로 단절, 시설 과부하가 기존 품질 계산을 통해 영향을 준다. 서비스 필드 갱신 및 한 틱 지연 구조와 STEP 3.3 비용·복지 상수는 유지했다.

## 틱·저장

`MacroSim.step()`에서 틱 증가 → 사건 처리 → 입주율·통계 평가 → 기존 필드 갱신·성장·재정 처리를 수행한다. 실시간과 기존 오프라인 캐치업은 같은 step을 사용한다.

난수는 기존 `simRandom`에 별도 채널 salt와 틱·좌표를 넣는다. 월드 시드, 지형 해시, 건물 ID, `isAnchor`, 청크 코덱은 변경하지 않는다. 사건 순회와 발생 후보는 정렬하여 청크 로딩 순서에 의존하지 않도록 한다.

도시 문서의 선택 필드 `macro.disasters`에 버전 1, 활성 사건 목록, 종류별 발생 수, 진압·전소 수를 저장한다. 사건은 앵커 좌표·건물 코드·건설일·발생 틱을 가진다. 타일별 위험도 배열이나 입주율은 저장하지 않는다. 필드 없는 옛 저장본은 사건 없음으로 읽으며 schema version은 유지한다. 잘못된 값, 중복, 미래 발생 시각, 철거된 건물의 사건을 정리한다.

`SaveManager`는 청크와 같은 시점의 매크로 복사본을 잡아 비동기 트랜잭션 재시도 중 후속 틱 상태가 섞이는 것을 막는다. Firestore 실계정 왕복은 이 환경에 설정이 없어 실행하지 않았다. 로컬 검사는 JSON 직렬화 후 사건 재개 및 물리 피해의 일치를 확인한다.

## 검사 및 범위

`npm run check:disaster`에서 자연 발생, 서비스 예방·회복, 전소, 경계 확산, 도로 차단, 같은 틱 연쇄 차단, 재개발 차단, 저장 재개, 이전 저장 호환, 매크로 통합을 검사한다. 시각 확인은 개발 서버의 `/tools/check/visual.html`에서 할 수 있다. 고정 시험 도시이며 로그인·저장하지 않고 ‘1시간 진행’으로 사건을 진행한다.

긴급 출동 차량, 질병 전염망, 전기·수도, 오염, 시설 노후화는 이 버전에 포함하지 않는다. 실제 대응 효과는 서비스 품질로 계산한다.
