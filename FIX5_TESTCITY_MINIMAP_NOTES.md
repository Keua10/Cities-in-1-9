# FIX5 — 테스트 도시 + 미니맵

## 테스트 도시
- 기존 저장 도시에 도로/지구가 하나라도 있으면 손대지 않는다.
- 완전히 빈 도시에서만 base 4x4 안의 20x20 연속 육지를 찾아 작은 테스트 도시를 결정론적으로 만든다.
- 3x3 도로 격자, 주거/상업/공업 지구, 일부 L1/L2 건물을 즉시 배치하므로 첫 접속부터 STEP 3.2 교통을 테스트할 수 있다.
- 테스트 도시 중심으로 첫 카메라를 맞춘다.

## 미니맵
- 좌하단에 고정 표시.
- 개척 청크 + 저장된 도로/지구만 읽는다. 미니맵 때문에 화면 밖 지형 청크를 생성하지 않는다.
- 흰 빈 사각형 = 현재 카메라가 보는 범위.
- 작은 미니맵을 누르면 해당 위치로 카메라를 이동시키고 큰 지도를 연다.
- 큰 지도는 화면의 약 92% x 92%를 차지한다.
- 큰 지도에서 탭/드래그하면 카메라 중심과 흰 사각형이 같이 움직인다.
- 우측 상단 X로 닫는다.
- 정적 지도는 500ms 주기로 다시 굽고, 카메라 사각형만 프레임마다 갱신한다.

## 앞으로의 맵 초기화 규칙
맵 생성 규칙, WORLD_SEED, base 배치/간격처럼 저장 좌표 해석이 달라지는 변경을 하는 STEP에서는 기존 도시 초기화를 그 STEP 패치에 같이 넣는다. 단순 UI/시뮬레이션 변경에는 저장 도시를 임의로 지우지 않는다.

## 검증
- 신규 `minimap.ts`, `testCity.ts`는 strict + noUnusedLocals/noUnusedParameters TypeScript 스텁 검사 통과.
- 이 환경에는 저장소 전체 node_modules가 없어서 실제 `npm run build`는 실행하지 못함.
