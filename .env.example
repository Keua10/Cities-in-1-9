# 이 파일을 .env 로 복사해서 값을 채우세요. (.env 는 커밋하지 마세요)
# Vercel 에는 Settings -> Environment Variables 에 같은 이름으로 넣으면 됩니다.
#
# Firebase 콘솔 -> 프로젝트 설정 -> 내 앱 -> 웹 앱의 firebaseConfig 에서 가져옵니다.
# 이 네 값은 원래 공개되는 값입니다. 보안은 firestore.rules 가 담당합니다.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=

# 학생 번호를 계정 이메일로 바꿀 때 붙는 도메인. 실제로 메일이 가지는 않습니다.
# 한 번 정하면 바꾸지 마세요 — 바꾸면 기존 계정으로 로그인이 안 됩니다.
VITE_STUDENT_DOMAIN=cities19.local

# 계정을 몰아서 만들 때만 내 컴퓨터에서 1 로 켭니다.
# Vercel 환경변수에는 절대 넣지 마세요. 넣으면 학생이 계정을 무한정 만들 수 있습니다.
VITE_ALLOW_SIGNUP=
