# Cities in 1-9 — STEP3 완료 내역

## 완료한 작업

- `public/sprites/buildings.png` 추가
  - 1152×384 투명 PNG
  - 1·2·3등급 × 주거·상업·공업 × A/B 변형 18종
  - 저해상도 도트 그래픽, 제한 팔레트, 2픽셀 단위 계단형 윤곽
  - 안티앨리어싱 없이 nearest-neighbor 확대에 맞게 제작
  - 각 셀의 아래 기준점을 건물 메시 앵커에 맞춤
- 도시 상태판 개선
  - 공실률 숫자와 입주 상태 게이지 추가
  - 입주율에 따라 정상·주의·위험 색상 표시
  - 자금·인구·수요·상태 안내 문구를 학생이 이해하기 쉽게 수정
- 개발 HUD 문구 정리
- 로그인 없이 실행할 때 시작 자금이 0원이던 문제 수정
- 도로 변경 신호가 최소 갱신 간격 전에 사라지던 문제 수정
- 장기 성장과 재건축이 실제로 이어지도록 경제·계층 비중 조정
- `tools/simcheck.ts`가 실제 시작 자금으로 재정·재건축·입주율을 검사하도록 보강

## 보존한 호환성 규칙

- `WORLD_SEED`, `TILE_W/H`, `HEIGHT_UNIT`, `CHUNK_SIZE` 유지
- 지형 생성 코드와 RLE 코덱 유지
- Build ID 0~3 유지
- 건물 코드 0~8, `BLD_NONE=255`, `BLD_COVERED=254` 유지
- `SCHEMA_VERSION=1` 유지
- 로그인 화면과 저장 배지 코드 유지
- 청크 메시·건물 메시의 기존 드로우콜 구조 유지

## 검증 결과

- `npx tsc --noEmit`: 통과
- `npx vite build`: 통과
- `tools/simcheck.ts`: 통과
  - 220게임일
  - 최종 인구 10,042명
  - 최종 입주율 78%
  - 재건축 과정에서 기존 건물 364채 철거 확인
  - 파산 없이 최종 자금 약 619만 원
- `tools/simcheck.ts`가 `dist`에 포함되지 않음을 확인
- `public/sprites/buildings.png`가 빌드 결과에 1152×384로 복사됨을 확인

## 참고

Vite 빌드에서 Firebase와 Pixi 번들이 500kB를 넘는다는 경고가 나오지만 빌드는
정상 완료된다. 이번 단계의 기능 오류는 아니며, 코드 분할 최적화가 필요할 때 별도
단계에서 처리하면 된다.
