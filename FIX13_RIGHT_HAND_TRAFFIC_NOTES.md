# FIX13 — 우측통행이 "화면에서" 확인되게 만들기 + 교차로/스폰 정리

## 먼저: 차선 계산은 원래 맞았다

FIX8~FIX12 에서 계속 고친 `진행방향의 오른쪽 = (dir + 1) & 3` 은 이 프로젝트의
아이소메트릭 정의(`iso.ts`)에서 **정답이다.**

```
+tx -> 화면 (+32, +16)  오른쪽아래
+ty -> 화면 (-32, +16)  왼쪽아래
```
y가 아래로 증가하는 화면에서 진행방향을 시계방향 90도 돌린 쪽이 운전자의
오른쪽이므로, +tx 의 오른쪽은 +ty 다. FIX11/FIX12 의 규칙은 네 방향 모두
맞았고, 이번 패치의 검증기(`tools/check/laneCheck.ts`)로 수치 확인했다.

**그런데도 화면에서 확인이 안 됐던 이유는 전부 렌더링 쪽이었다.**
계산을 계속 고쳐도 화면이 그대로였으니 같은 수정을 반복하게 된 것이다.

## 화면에서 안 보였던 진짜 원인 4가지

1. **차선 간격이 스프라이트보다 좁았다.**
   `LANE_OFFSET_TILES = 0.20` 이면 마주 오는 두 차의 화면 거리가 약 14px 인데,
   차량 스프라이트는 20px 사각형이었다. 계산상으로는 갈라져 있어도 그림이 서로 덮었다.
   -> 도로 폭이 1타일이므로 한 차선의 중심은 **0.25** 가 정답. 그렇게 고쳤다.

2. **fallback 차량이 방향을 구분할 수 없는 그림이었다.**
   네 방향 모두 같은 화면축 정렬 사각형이고 팔레트 색만 달랐다. 차가 어디로
   가는지 알 수 없으니 "우측통행인지" 자체를 눈으로 판정할 수 없었다.
   -> 타일 평면의 두 축으로 **실제 아이소메트릭 차량**을 그린다. 앞유리/전조등이
   진행방향에, 후미등이 뒤에 온다. 방향이 한눈에 읽힌다.

3. **깊이 정렬을 도로 중앙선으로 했다.**
   `depth = tx + ty + tileT` 라서 마주 오는 두 차의 깊이가 사실상 같았고,
   매 프레임 앞뒤가 뒤집히며 깜빡였다. -> 실제 차선 위치로 정렬한다.

4. **트럭 그림이 셀 밖으로 넘쳐 승용차 칸에 찍혔다.**
   아틀라스 셀(32px)보다 트럭이 커서 아랫줄 지붕이 윗줄 칸에 섞였다.
   -> 셀 단위 clip + 접지점을 셀 중심에서 5px 아래로 내렸다.

## 그 외에 같이 고친 것

### 교차로/코너 겹침
예전에는 "교차로 타일 하나에 차 한 대" 였다. 그래서
- L자 코너에서 **반대 차선 차까지 서로 막아** 통행량이 반토막 났고,
- 어느 차선인지 읽히기 전에 양쪽이 번갈아 멈춰 섰다.

지금은 타일마다 "지금 지나가는 움직임(진입방향 -> 진출방향)" 목록을 두고
**실제 궤적 선분이 교차할 때만** 막는다. `movementsConflict()` 가 판정한다.

| 상황 | 예전 | 지금 |
| --- | --- | --- |
| 마주 오는 직진끼리 | 막힘 | 통과 |
| 마주 오는 좌회전끼리 | 막힘 | 통과 |
| L자 코너 양방향 | 막힘 | 통과 |
| 직교 직진끼리 | 막힘 | 막힘 |
| 좌회전 vs 마주 오는 직진 | 막힘 | 막힘 |
| 좌회전 vs 마주 오는 우회전(같은 차선 합류) | 막힘 | 막힘 |

### 회전 궤적
코너에서 진입/진출 차선의 **교점을 2차 베지에 제어점**으로 쓴다. 제어점이 곧
교점이라 직선 구간과 접선이 정확히 이어지고, 우회전은 안쪽으로 짧게,
좌회전은 바깥으로 넓게 돈다. (검증: 우회전 경로 길이 < 좌회전 경로 길이)

### 차량 위치가 한 타일 튀던 버그
`routeIdx = points - 1` (마지막 노드)에서 `lanePosition` 이 인덱스만 자르고
`tileT` 는 그대로 써서 위치가 한 타일 뒤로 튀었다. 진행도(`routeIdx + tileT`)를
통째로 자르도록 고쳤다.

### 앞차 간격
2타일만 봤다. 원하는 간격(차체 0.50 + 여유 0.55 = 1.05타일)에 제동거리를
더하면 2타일을 넘으므로 신호 앞 대기열에서 뒤차가 앞차를 파고들 수 있었다.
-> **3타일 앞까지** 본다.

### "특정 시간대에 한꺼번에 생성" 
원인이 두 개였다.

1. `spawnTokens` 상한이 `MAX_SPAWNS_PER_SEC = 90` 이었다. 조용한 구간에
   토큰이 90개까지 차고, 큐가 생기면 **90대가 한 번에** 나갈 수 있는 구조였다.
   -> 토큰 버킷 상한을 `SPAWN_BURST_TOKENS = 3` 으로. 이 값이 곧
   "한 번에 나올 수 있는 최대 대수" 다.
2. 반대로 전역 380~850ms 대기 때문에 실효 상한이 **초당 1.6대**였다.
   출근 시각에 잡힌 수백 건이 큐에 남아 하루 종일 한 줄로 흘러나온다.
   사람 눈에는 그게 "한꺼번에" 로 보인다.
   -> 평균 속도만 `SPAWN_RATE_PER_SEC = 14` 로 두고, 뭉침 방지는 진입로별
   700ms 간격이 맡는다. 대기열이 깊을수록 분산 창을 넓힌다(최대 9초).

또 `readySpawns` 를 매번 `sort` 하던 것을 이진 삽입으로 바꿨다.

### 속도
`VEHICLE_SPEED_TILES_PER_SEC` 8.0 -> 6.0. 8타일/초는 화면에서 초당 286px 라
차선을 눈으로 따라갈 수 없었다.

## 바뀐 파일

```
src/sim/traffic/laneGeometry.ts   재작성 (차선/코너/충돌 판정)
src/sim/traffic/trafficSim.ts     교차로 예약, 앞차 간격, 스폰 페이싱
src/sim/simConstants.ts           차선/차체/스폰 상수
src/render/vehicleAtlas.ts        방향이 보이는 아이소메트릭 fallback 차량
src/render/vehicleMesh.ts         1:1 픽셀, 접지점 기준, 차선 기준 정렬
tools/check/laneCheck.ts          검증기 (빌드에 포함되지 않음)
tools/check/stub-pixi.ts          검증기용 pixi 스텁
```

## 검증하는 법

```bash
npm i -D @napi-rs/canvas
npx esbuild tools/check/laneCheck.ts --bundle --platform=node --format=esm \
  --alias:pixi.js=./tools/check/stub-pixi.ts --external:@napi-rs/canvas \
  --outfile=laneCheck.mjs
node laneCheck.mjs
```

- 콘솔: 우측통행 4방향, 코너 연속성, 교차로 충돌 진리표 20개 항목
- 그림: `/tmp/lane_scene.png`, `/tmp/lane_zoom.png`, `/tmp/vehicle_atlas.png`
  (확대 그림에서 노란 점선 = 도로 중앙선. 양쪽 차가 갈라져 있어야 한다)

## 유지된 것

- `MAX_ACTIVE_VEHICLES = 1000`
- daytime 07:30 테스트 시작 / 600초 = 1일
- 기존 저장 포맷 / 건물 ID / A* 비용 구조
